{
  "name": "gptmaker-frontend",
  "version": "1.0.0",
  "description": "Frontend for GPT Maker chat application",
  "scripts": {
    "start": "serve -s . -l $PORT --config serve.json",
    "dev": "serve -l 5500 . --config serve.json",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}